---
title: 'hyve template'
description: 'Complete reference for all cluster template management commands'
---

The `hyve template` command group manages reusable cluster templates that combine cluster specifications with automated workflows. Templates enable you to quickly create standardized clusters with pre-configured deployments.

## Overview

Templates are declarative definitions that specify cluster configuration and associated workflows to run after creation. They enable consistent, repeatable cluster deployments across environments.

## Commands

### hyve template create

Create a new cluster template with cluster specifications and workflows.

```bash
hyve template create [template-name] [flags]
```

<ParamField path="template-name" type="string" required>
  Name of the template to create
</ParamField>

<ParamField path="--description" type="string">
  Template description
</ParamField>

<ParamField path="--provider" type="string" default="civo">
  Cloud provider (civo, aws, gcp, azure)
</ParamField>

<ParamField path="--region" type="string" default="PHX1">
  Cloud provider region
</ParamField>

<ParamField path="--nodes" type="string" default="g4s.kube.small">
  Node sizes (comma-separated). Each entry creates one node.
</ParamField>

<ParamField path="--cluster-type" type="string" default="k3s">
  Kubernetes cluster type (`k3s`, `talos`). **Civo only** — ignored for AWS, GCP, and Azure providers.
</ParamField>

<ParamField path="--ingress" type="boolean" default="true">
  Enable ingress controller
</ParamField>

<ParamField path="--load-balancer" type="boolean" default="true">
  Enable load balancer for ingress
</ParamField>

<ParamField path="--on-create, -c" type="string">
  Workflows to run after cluster creation (comma-separated)
</ParamField>

<ParamField path="--on-destroy" type="string">
  Workflows to run before cluster destruction (comma-separated)
</ParamField>

<ParamField path="--schedule" type="string">
  A 5-field cron expression (e.g. `0 20 * * 5`) that sets an automatic expiry on every cluster created from this template. When the template is executed, the next occurrence of the schedule is evaluated and written to the cluster's `spec.expiresAt` field as an RFC 3339 timestamp. Leave blank for clusters that should not expire automatically.

  Standard cron field order: `<minute> <hour> <day-of-month> <month> <day-of-week>`

  | Field | Range | Special values |
  |-------|-------|----------------|
  | Minute | 0–59 | `*` = any |
  | Hour | 0–23 | `*` = any |
  | Day of month | 1–31 | `*` = any |
  | Month | 1–12 | `*` = any |
  | Day of week | 0–6 (Sun=0) | `*` = any; ranges (`1-5`) and lists (`1,3,5`) supported |

  Examples: `0 20 * * 5` (every Friday at 20:00), `0 0 * * *` (daily at midnight)
</ParamField>

**Example:**

```bash
# Create basic template with lifecycle workflows
hyve template create prod-template \
  --description "Production cluster template" \
  --region NYC1 \
  --nodes g4s.kube.large,g4s.kube.large,g4s.kube.large \
  --on-create setup-monitoring,deploy-app \
  --on-destroy backup-data,cleanup

# Create a template with an automatic weekly Friday expiry
hyve template create dev-weekly \
  --region PHX1 \
  --nodes g4s.kube.small \
  --schedule "0 20 * * 5"

# Create minimal template
hyve template create dev-template \
  --region PHX1 \
  --nodes g4s.kube.small \
  --ingress=false
```

**Output:**

```
✅ Template 'prod-template' created successfully
Template path: /Users/username/.hyve/repositories/production/templates/prod-template.yaml

📋 Template Details:
  Provider: civo
  Region: NYC1
  Nodes: g4s.kube.large, g4s.kube.large, g4s.kube.large
  Cluster Type: k3s
  Ingress: true
  OnCreate Workflows: setup-monitoring, deploy-app
  OnDestroy Workflows: backup-data, cleanup

💡 Execute this template with:
  hyve template execute prod-template <cluster-name>
```

---

### hyve template list

Display all available cluster templates in the current repository.

```bash
hyve template list
```

**Example:**

```bash
hyve template list
```

**Output:**

```
📋 Available templates (3):

  prod-template
    Description: Production cluster template
    Region: NYC1 | Nodes: g4s.kube.large, g4s.kube.large, g4s.kube.large | Type: k3s
    OnCreate Workflows: setup-monitoring, deploy-app
    OnDestroy Workflows: backup-data, cleanup

  staging-template
    Description: Staging environment template
    Region: PHX1 | Nodes: g4s.kube.medium, g4s.kube.medium | Type: k3s
    OnCreate Workflows: setup-monitoring

  dev-template
    Region: PHX1 | Nodes: g4s.kube.small | Type: k3s

💡 Execute a template with:
  hyve template execute <template-name> <cluster-name>
```

---

### hyve template show

Display the full details of a cluster template.

```bash
hyve template show [template-name]
```

<ParamField path="template-name" type="string" required>
  Name of the template to display
</ParamField>

**Example:**

```bash
hyve template show prod-template
```

**Output:**

```
📋 Template: prod-template

apiVersion: v1
kind: Template
metadata:
  name: prod-template
  description: Production cluster template
spec:
  provider: civo
  region: NYC1
  nodes:
    - g4s.kube.large
    - g4s.kube.large
    - g4s.kube.large
  clusterType: k3s
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - deploy-app
    onDestroy:
      - backup-data
```

---

### hyve template delete

Remove a cluster template from the repository.

```bash
hyve template delete [template-name]
```

<ParamField path="template-name" type="string" required>
  Name of the template to delete
</ParamField>

**Example:**

```bash
hyve template delete old-template
```

**Output:**

```
✅ Template 'old-template' deleted successfully
```

---

### hyve template execute

Create a cluster from a template and run associated workflows.

```bash
hyve template execute [template-name] [cluster-name]
```

<ParamField path="template-name" type="string" required>
  Name of the template to execute
</ParamField>

<ParamField path="cluster-name" type="string" required>
  Name for the new cluster
</ParamField>

**Provider-specific flags** (override template values; required if not set in the template):

| Flag | Short | Provider | Description |
|------|-------|----------|-------------|
| `--org` | `-o` | Civo | Civo organization alias |
| `--account` | `-a` | AWS | AWS account alias |
| `--vpc-id` | `-v` | AWS | AWS VPC ID |
| `--eks-role-name` | `-e` | AWS | AWS EKS cluster role name |
| `--node-role-name` | `-n` | AWS | AWS node role name |
| `--subscription` | `-s` | Azure | Azure subscription alias |
| `--resource-group` | `-g` | Azure | Azure resource group name |
| `--project` | `-p` | GCP | GCP project alias |

**Example:**

```bash
# Civo template (org set in template YAML — no flags needed)
hyve template execute prod-template prod-cluster-01

# Civo template with org override
hyve template execute prod-template prod-cluster-01 --org my-org

# AWS template with all required flags
hyve template execute aws-template eks-prod-01 \
  --account prod \
  --vpc-id vpc-0abc123456789 \
  --eks-role-name eks-role \
  --node-role-name node-role

# Azure template with overrides
hyve template execute azure-template aks-prod-01 \
  --subscription prod-sub \
  --resource-group hyve-rg
```

**Output:**

```
🚀 Executing template 'prod-template' to create cluster 'prod-cluster-01'...

📋 Template Details:
  Provider: civo
  Region: NYC1
  Nodes: g4s.kube.large, g4s.kube.large, g4s.kube.large
  Cluster Type: k3s
  OnCreate Workflows: setup-monitoring, deploy-app
  OnDestroy Workflows: backup-data

✅ Cluster definition created: /Users/username/.hyve/repositories/production/clusters/prod-cluster-01.yaml

1️⃣ Creating cluster...
✅ Cluster 'prod-cluster-01' created successfully (ID: abc123)

2️⃣ Waiting for cluster to be ready...
Polling cluster status (this may take several minutes)...
✅ Cluster 'prod-cluster-01' is ready

3️⃣ Syncing kubeconfig...
✅ Kubeconfig synced and stored for cluster 'prod-cluster-01'

4️⃣ Executing 2 onCreate workflow(s)...

[1/2] Running workflow: setup-monitoring
[10:30:45][INFO][install-prometheus] Starting job 'install-prometheus'
[10:31:20][INFO][install-prometheus] Job completed successfully
[10:31:20][INFO][install-grafana] Starting job 'install-grafana'
[10:31:45][INFO][install-grafana] Job completed successfully
✅ Workflow 'setup-monitoring' completed successfully

[2/2] Running workflow: deploy-app
[10:32:00][INFO][pre-deployment] Starting job 'pre-deployment'
[10:32:05][INFO][pre-deployment] Job completed successfully
[10:32:05][INFO][deploy] Starting job 'deploy'
[10:32:30][INFO][deploy] Job completed successfully
✅ Workflow 'deploy-app' completed successfully

✅ Template execution completed!

💡 Cluster 'prod-cluster-01' is now available
💡 Use 'hyve kubeconfig sync' to get the kubeconfig
💡 OnDestroy workflows will run when the cluster is deleted
```

**Execution Process:**

1. **Create Cluster Definition**: Generate YAML file from template
2. **Provision Cluster**: Create cluster in cloud provider
3. **Wait for Ready**: Poll cluster status until ready
4. **Sync Kubeconfig**: Retrieve and store kubeconfig
5. **Execute Workflows**: Run each workflow in sequence

<Info>
  Template execution is idempotent. If the cluster already exists, it will be reused and workflows will run on the existing cluster.
</Info>

---

### hyve template validate

Validate the syntax and structure of a cluster template definition.

```bash
hyve template validate [template-name]
```

<ParamField path="template-name" type="string" required>
  Name of the template to validate
</ParamField>

**Example:**

```bash
hyve template validate prod-template
```

**Output (Success):**

```
🔍 Validating template 'prod-template'...

✅ Template is valid
📋 Provider: civo
📋 Region: NYC1
📋 Nodes: 3 (g4s.kube.large, g4s.kube.large, g4s.kube.large)
📋 Cluster Type: k3s
📋 Ingress: true
📋 OnCreate Workflows: 2 (setup-monitoring, deploy-app)
📋 OnDestroy Workflows: 1 (backup-data)
✨ No warnings
```

**Output (Errors):**

```
🔍 Validating template 'broken-template'...

❌ Validation Failed

Errors:
  • Missing apiVersion
  • Missing spec.region
  • Missing spec.nodes or spec.nodeGroups (at least one required)
  • Invalid kind 'Cluster', expected 'Template'

⚠️  Warnings:
  • Region 'INVALID' may not be valid for Civo. Valid regions: PHX1, NYC1, FRA1, LON1
  • Node size 'g4s.kube.huge' may not be valid for Civo
  • OnCreate workflow 'missing-workflow' not found in repository
```

**Validation Checks:**

- Required fields (apiVersion, kind, metadata.name)
- Valid provider and region combinations
- At least one node specified
- Valid node sizes for provider
- Cluster type is supported
- Referenced workflows exist

---

## Template Definition Format

Templates are defined in YAML format and stored in the `templates/` directory.

### Basic Structure

```yaml
apiVersion: v1
kind: Template
metadata:
  name: prod-template
  description: Production cluster template
spec:
  provider: civo
  region: NYC1
  nodes:
    - g4s.kube.large
    - g4s.kube.large
    - g4s.kube.large
  clusterType: k3s
  schedule: "0 20 * * 5"    # optional: auto-expire clusters at next Friday 20:00
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - deploy-app
    onDestroy:
      - backup-data
```

### Field Reference

<ParamField path="apiVersion" type="string" required>
  API version of the template definition (currently `v1`)
</ParamField>

<ParamField path="kind" type="string" required>
  Resource kind (must be `Template`)
</ParamField>

<ParamField path="metadata.name" type="string" required>
  Unique name for the template
</ParamField>

<ParamField path="metadata.description" type="string">
  Human-readable description
</ParamField>

<ParamField path="spec.provider" type="string" required>
  Cloud provider (civo, aws, gcp, azure)
</ParamField>

<ParamField path="spec.region" type="string" required>
  Cloud provider region
</ParamField>

<ParamField path="spec.nodes" type="string[]">
  Array of node sizes for simple single-pool clusters (Civo). Each entry creates one node. Either `nodes` or `nodeGroups` must be specified.
</ParamField>

<ParamField path="spec.nodeGroups" type="object[]">
  Named node pools for AWS EKS, GCP GKE, and Azure AKS. Use instead of `nodes` when you need multiple pools, autoscaling, spot instances, or per-pool labels and taints. Each entry supports:

  | Field | Type | Description |
  |-------|------|-------------|
  | `name` | string | Pool name (required) |
  | `instanceType` | string | Node instance/machine type (required) |
  | `count` | integer | Desired node count |
  | `minCount` | integer | Minimum nodes for autoscaling |
  | `maxCount` | integer | Maximum nodes for autoscaling |
  | `diskSize` | integer | Root disk size in GB |
  | `spot` | boolean | Use spot/preemptible instances |
  | `mode` | string | Azure only: `System` or `User` pool |
  | `labels` | map | Key-value node labels |
  | `taints` | object[] | Node taints (`key`, `value`, `effect`) |
</ParamField>

<ParamField path="spec.clusterType" type="string" default="k3s">
  Kubernetes distribution type (`k3s`, `talos`). **Civo only** — this field is ignored for AWS (EKS), GCP (GKE), and Azure (AKS) providers.
</ParamField>

<ParamField path="spec.ingress.enabled" type="boolean" default="true">
  Enable ingress controller installation
</ParamField>

<ParamField path="spec.ingress.loadBalancer" type="boolean" default="true">
  Enable load balancer for ingress
</ParamField>

<ParamField path="spec.workflows.beforeCreate" type="string[]">
  Names of workflows to run before the cluster is provisioned. **No kubeconfig is injected** — the cluster does not exist yet. Set via `--before-create` flag or the interactive TUI.
</ParamField>

<ParamField path="spec.workflows.onCreate" type="string[]">
  Names of workflows to execute after cluster creation. Kubeconfig is injected automatically. Set via `--on-create` flag or the interactive TUI.
</ParamField>

<ParamField path="spec.workflows.onDestroy" type="string[]">
  Names of workflows to execute before cluster destruction. Kubeconfig is injected automatically. Set via `--on-destroy` flag or the interactive TUI.
</ParamField>

<ParamField path="spec.workflows.afterDelete" type="string[]">
  Names of workflows to run after the cluster is deleted. **No kubeconfig is injected** — the cluster no longer exists. Set via `--after-delete` flag or the interactive TUI.
</ParamField>

<ParamField path="spec.schedule" type="string">
  A 5-field cron expression (e.g. `0 20 * * 5`). When the template is executed, the next occurrence after the current time is computed and written to the generated cluster's `spec.expiresAt` as an RFC 3339 timestamp. The cluster is then automatically deleted when that deadline passes. Omit this field for clusters that should run indefinitely.
</ParamField>

### AWS-Specific Fields

These fields apply when `spec.provider` is `aws`.

<ParamField path="spec.awsAccount" type="string">
  AWS account alias name as registered via `hyve config aws account add`.
</ParamField>

<ParamField path="spec.awsVpcId" type="string">
  AWS VPC ID. Set via `--vpc-id/-v` at execute time or in the template YAML directly. The interactive TUI offers a live cloud lookup to select from available VPCs.
</ParamField>

<ParamField path="spec.awsEksRoleName" type="string">
  IAM role name for the EKS control plane. Set via `--eks-role-name/-e` at execute time or in the template YAML. The role ARN is constructed at reconcile time from this name and the account ID.
</ParamField>

<ParamField path="spec.awsNodeRoleName" type="string">
  IAM role name for EKS node groups. Set via `--node-role-name/-n` at execute time or in the template YAML.
</ParamField>

<Info>
  AWS VPC and role fields can be set in the template YAML or overridden at execute time via flags. The interactive TUI provides live cloud lookup for VPCs and IAM roles.
</Info>

<Info>
  **EKS instance type:** avoid `nano` and `micro` instance types (e.g. `t3.micro`, `t2.micro`) for EKS node groups. These instances support only 4 pods maximum (2 ENIs × 2 IPs), which is not enough to run EKS system daemonsets (`aws-node`, `kube-proxy`) alongside workloads. Use **`t3.small`** (11 pods) at minimum, or **`t3.medium`** (17 pods) as the recommended baseline.
</Info>

---

## Common Use Cases

### Production Template with Monitoring

```yaml
apiVersion: v1
kind: Template
metadata:
  name: production-ha
  description: High-availability production cluster with full monitoring stack
spec:
  provider: civo
  region: NYC1
  nodes:
    - g4s.kube.xlarge
    - g4s.kube.xlarge
    - g4s.kube.xlarge
  clusterType: k3s
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - install-cert-manager
      - configure-backups
      - deploy-app
    onDestroy:
      - final-backup
      - notify-team
```

**Execute:**

```bash
hyve template execute production-ha prod-us-east-01
```

---

### Development Template

```yaml
apiVersion: v1
kind: Template
metadata:
  name: dev-minimal
  description: Minimal development cluster
spec:
  provider: civo
  region: PHX1
  nodes:
    - g4s.kube.small
  clusterType: k3s
  ingress:
    enabled: false
    loadBalancer: false
  workflows:
    onCreate:
      - setup-dev-tools
```

**Execute:**

```bash
hyve template execute dev-minimal dev-feature-auth
```

---

### Multi-Region Template Set

```bash
# Create templates for each region
hyve template create us-east \
  --region NYC1 \
  --nodes g4s.kube.large,g4s.kube.large \
  --on-create setup-monitoring

hyve template create us-west \
  --region PHX1 \
  --nodes g4s.kube.large,g4s.kube.large \
  --on-create setup-monitoring

hyve template create eu-central \
  --region FRA1 \
  --nodes g4s.kube.large,g4s.kube.large \
  --on-create setup-monitoring

# Deploy clusters from templates
hyve template execute us-east prod-us-east-01
hyve template execute us-west prod-us-west-01
hyve template execute eu-central prod-eu-central-01
```

---

### Staging Environment

```yaml
apiVersion: v1
kind: Template
metadata:
  name: staging
  description: Staging environment template
spec:
  provider: civo
  region: PHX1
  nodes:
    - g4s.kube.medium
    - g4s.kube.medium
  clusterType: k3s
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - configure-staging
      - deploy-app
```

---

## Template Best Practices

### 1. Naming Conventions

Use descriptive names that indicate purpose and scale:

```bash
# Good examples
prod-ha-monitoring      # Production, high-availability with monitoring
staging-standard        # Standard staging setup
dev-minimal            # Minimal development cluster
testing-ephemeral      # Temporary testing cluster
```

### 2. Workflow Organization

Order workflows from infrastructure to application:

```yaml
workflows:
  onCreate:
    - setup-networking      # Network policies, CNI config
    - install-cert-manager  # Certificate management
    - setup-monitoring      # Prometheus, Grafana
    - configure-storage     # PV, PVC, storage classes
    - deploy-app           # Application deployment
  onDestroy:
    - backup-data          # Final backup before deletion
    - cleanup-dns          # Remove DNS records
```

### 3. Resource Sizing

Match node sizes to environment purpose:

```yaml
# Development
nodes:
  - g4s.kube.small

# Staging
nodes:
  - g4s.kube.medium
  - g4s.kube.medium

# Production
nodes:
  - g4s.kube.large
  - g4s.kube.large
  - g4s.kube.large
```

### 4. Template Reuse

Create base templates and extend them:

```bash
# Base template
hyve template create base-cluster \
  --region PHX1 \
  --nodes g4s.kube.medium \
  --on-create setup-base

# Extended for specific use case
# Copy and modify base-cluster.yaml to add specific workflows
```

---

## Template Library

### Minimal Single-Node

```yaml
apiVersion: v1
kind: Template
metadata:
  name: minimal
spec:
  provider: civo
  region: PHX1
  nodes:
    - g4s.kube.small
  clusterType: k3s
  ingress:
    enabled: false
```

---

### Standard 3-Node Cluster

```yaml
apiVersion: v1
kind: Template
metadata:
  name: standard-3node
spec:
  provider: civo
  region: NYC1
  nodes:
    - g4s.kube.medium
    - g4s.kube.medium
    - g4s.kube.medium
  clusterType: k3s
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
```

---

### AWS EKS Cluster

```yaml
apiVersion: v1
kind: Template
metadata:
  name: aws-eks-standard
  description: Standard AWS EKS cluster with named node groups
spec:
  provider: aws
  region: us-east-1
  nodeGroups:
    - name: system
      instanceType: t3.medium
      count: 2
      minCount: 1
      maxCount: 4
    - name: workers
      instanceType: t3.large
      count: 3
      minCount: 1
      maxCount: 10
      spot: true
      labels:
        role: worker
  clusterType: eks
  awsAccount: main-account      # alias from hyve config aws account add
  awsVpcId: vpc-0abc123456789   # VPC ID (or override at execute time with --vpc-id)
  awsEksRoleName: k8srole       # EKS cluster role name (ARN constructed at reconcile time)
  awsNodeRoleName: k8snoderole  # EKS node role name
  ingress:
    enabled: false
    loadBalancer: false
  workflows:
    onCreate:
      - deploy-app
```

**Execute:**

```bash
hyve template execute aws-eks-standard eks-prod-01

# Or override VPC and roles at execution time:
hyve template execute aws-eks-standard eks-prod-01 \
  --vpc-id vpc-0def987654321 \
  --eks-role-name prod-eks-role
```

---

### GCP GKE Cluster

```yaml
apiVersion: v1
kind: Template
metadata:
  name: gcp-gke-standard
  description: GCP GKE cluster with separate system and worker node pools
spec:
  provider: gcp
  region: us-central1
  nodeGroups:
    - name: system-pool
      instanceType: e2-standard-2
      count: 2
      minCount: 1
      maxCount: 3
    - name: worker-pool
      instanceType: e2-standard-4
      count: 3
      minCount: 1
      maxCount: 10
      labels:
        role: worker
  clusterType: gke
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - deploy-app
```

**Execute:**

```bash
hyve template execute gcp-gke-standard gke-prod-01
```

---

### Azure AKS Cluster

```yaml
apiVersion: v1
kind: Template
metadata:
  name: azure-aks-standard
  description: Azure AKS cluster with System and User agent pool profiles
spec:
  provider: azure
  region: eastus
  nodeGroups:
    - name: systempool
      instanceType: Standard_D2s_v3
      count: 2
      minCount: 1
      maxCount: 3
      mode: System
    - name: workerpool
      instanceType: Standard_D4s_v3
      count: 3
      minCount: 1
      maxCount: 10
      mode: User
      labels:
        role: worker
  clusterType: aks
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - deploy-app
```

**Execute:**

```bash
hyve template execute azure-aks-standard aks-prod-01
```

---

### High-Availability Production

```yaml
apiVersion: v1
kind: Template
metadata:
  name: production-ha
spec:
  provider: civo
  region: NYC1
  nodes:
    - g4s.kube.xlarge
    - g4s.kube.xlarge
    - g4s.kube.xlarge
  clusterType: k3s
  ingress:
    enabled: true
    loadBalancer: true
  workflows:
    onCreate:
      - setup-monitoring
      - install-cert-manager
      - configure-backups
      - setup-logging
      - deploy-app
    onDestroy:
      - final-backup
      - export-logs
      - notify-team
```

---

## Storage

Template definitions are stored in:

```
~/.hyve/repositories/<repository-name>/
└── templates/
    ├── prod-template.yaml
    ├── staging-template.yaml
    └── dev-template.yaml
```

---

## Related Commands

- [hyve cluster](/cli/cluster) - Templates create cluster definitions
- [hyve workflow](/cli/workflow) - Templates can execute workflows
- [hyve git](/cli/git) - Templates are stored in Git repositories
