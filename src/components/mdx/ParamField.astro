---
interface Props {
  path?: string;
  query?: string;
  body?: string;
  header?: string;
  type?: string;
  required?: boolean;
  default?: string;
}
const props = Astro.props as Props;
const name = props.path ?? props.query ?? props.body ?? props.header ?? '';
const { type, required = false, default: defaultValue } = props;
---
<div class="mdx-paramfield">
  <div class="mdx-paramfield-head">
    <code class="mdx-paramfield-name">{name}</code>
    {type && <span class="mdx-paramfield-type">{type}</span>}
    {required && <span class="mdx-paramfield-required">required</span>}
    {defaultValue && <span class="mdx-paramfield-default">default: {defaultValue}</span>}
  </div>
  <div class="mdx-paramfield-body">
    <slot />
  </div>
</div>
<style>
  .mdx-paramfield {
    border-top: 1px solid var(--sl-color-gray-5);
    padding: 0.75rem 0;
  }
  .mdx-paramfield-head {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.35rem;
  }
  .mdx-paramfield-name {
    font-weight: 600;
    font-size: 0.9rem;
  }
  .mdx-paramfield-type {
    font-size: 0.8rem;
    color: var(--sl-color-gray-3);
  }
  .mdx-paramfield-required {
    font-size: 0.7rem;
    color: var(--sl-color-orange);
    border: 1px solid currentColor;
    border-radius: 0.25rem;
    padding: 0 0.35rem;
  }
  .mdx-paramfield-default {
    font-size: 0.75rem;
    color: var(--sl-color-gray-3);
  }
  .mdx-paramfield-body {
    font-size: 0.9rem;
    color: var(--sl-color-gray-2);
  }
  .mdx-paramfield-body :global(p:first-child) {
    margin-top: 0;
  }
  .mdx-paramfield-body :global(p:last-child) {
    margin-bottom: 0;
  }
</style>
