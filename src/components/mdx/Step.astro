---
interface Props {
  title?: string;
  icon?: string;
}
const { title } = Astro.props as Props;
---
<div class="mdx-step">
  <div class="mdx-step-marker"></div>
  <div class="mdx-step-content">
    {title && <p class="mdx-step-title">{title}</p>}
    <slot />
  </div>
</div>
<style>
  .mdx-step {
    display: flex;
    gap: 1rem;
    position: relative;
    padding-bottom: 1.5rem;
  }
  .mdx-step:not(:last-child)::before {
    content: '';
    position: absolute;
    left: 0.65rem;
    top: 1.7rem;
    bottom: 0;
    width: 1px;
    background: var(--sl-color-gray-5);
  }
  .mdx-step-marker {
    flex-shrink: 0;
    width: 1.35rem;
    height: 1.35rem;
    border-radius: 50%;
    background: var(--sl-color-black);
    border: 1px solid var(--sl-color-gray-5);
    counter-increment: mdx-step;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--sl-color-white);
  }
  .mdx-step-marker::before {
    content: counter(mdx-step);
  }
  .mdx-step-content {
    flex: 1;
    min-width: 0;
    padding-top: 0.1rem;
  }
  .mdx-step-title {
    font-weight: 600;
    margin: 0 0 0.4rem 0;
  }
  .mdx-step-content :global(p:first-child) {
    margin-top: 0;
  }
</style>
