---
interface Props {
  cols?: number;
}
const { cols = 2 } = Astro.props as Props;
---
<div class="mdx-card-group" style={`--cols: ${cols}`}>
  <slot />
</div>
<style>
  .mdx-card-group {
    display: grid;
    grid-template-columns: repeat(var(--cols, 2), minmax(0, 1fr));
    grid-auto-rows: 1fr;
    align-items: stretch;
    gap: 1rem;
    margin: 1.5rem 0;
  }
  @media (max-width: 640px) {
    .mdx-card-group {
      grid-template-columns: 1fr;
    }
  }
</style>
