---
interface Props {
  title: string;
}
const { title } = Astro.props as Props;
const inputId = `tab-${Math.random().toString(36).slice(2, 9)}`;
---
<input type="radio" class="mdx-tab-input" id={inputId} style="display:none" />
<label for={inputId} class="mdx-tab-label">{title}</label>
<div class="mdx-tab-panel">
  <slot />
</div>
<style>
  .mdx-tab-label {
    display: inline-block;
    padding: 0.4rem 0.9rem;
    cursor: pointer;
    border-bottom: 2px solid transparent;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--sl-color-gray-2);
    order: 1;
  }
  .mdx-tab-panel {
    display: none;
    order: 99;
    width: 100%;
  }
  .mdx-tab-input:checked + .mdx-tab-label {
    border-bottom-color: var(--sl-color-accent);
    color: var(--sl-color-white);
  }
  .mdx-tab-input:checked + .mdx-tab-label + .mdx-tab-panel {
    display: block;
  }
</style>
