---
title: "Hyve"
description: "Kubernetes cluster lifecycle management — local CLI or cluster-native controller"
template: splash
editUrl: false
hero:
  title: Hyve
  tagline: Kubernetes cluster lifecycle management — as a local CLI, or as a cluster-native controller for your team.
  image:
    dark: ../../assets/banner-dark.svg
    light: ../../assets/banner-light.svg
    alt: Worker bees in hard hats assembling the Hyve logo block by block
  actions:
    - text: Get Started
      link: /docs/quickstart
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/cbridges1/hyve
      icon: external
      variant: secondary
---

import Card from '../../components/mdx/Card.astro';
import CardGroup from '../../components/mdx/CardGroup.astro';
import Steps from '../../components/mdx/Steps.astro';
import Step from '../../components/mdx/Step.astro';
import Accordion from '../../components/mdx/Accordion.astro';
import AccordionGroup from '../../components/mdx/AccordionGroup.astro';

<div style={{ textAlign: 'center', fontSize: '1.15rem', color: 'var(--sl-color-gray-2)', maxWidth: '640px', margin: '-1.5rem auto 1.5rem' }}>
  Define clusters as YAML, reconcile with one command. Same engine, any cloud provider, whether you run it locally or deploy it as a controller for your team.
</div>

<div style={{ maxWidth: '480px', margin: '0 auto 1rem' }}>
```bash
go install github.com/cbridges1/hyve@latest
```
</div>

<div style={{ display: 'flex', flexWrap: 'wrap', justifyContent: 'center', gap: '0.5rem', margin: '0 auto 3rem', maxWidth: '640px' }}>
  {['Civo', 'AWS EKS', 'GCP GKE', 'Azure AKS', '+ community modules'].map((p) => (
    <span style={{ border: '1px solid var(--sl-color-hairline)', borderRadius: '999px', padding: '0.25rem 0.9rem', fontSize: '0.85rem', color: 'var(--sl-color-gray-2)' }}>{p}</span>
  ))}
</div>

## Clusters as code

Terraform-style declarative provisioning, with a local directory as the state backend — git-backed if you want version control and review, a plain directory if you don't. A cluster is one YAML file: create it and the cluster is created, edit it and the cluster is updated, remove it and the cluster is deleted, the next time `hyve reconcile` runs.

```yaml
# clusters/my-cluster.yaml — in your active environment's directory
apiVersion: hyve.io/v1alpha1
kind: ClusterDefinition
metadata:
  name: my-cluster
spec:
  region: us-east-1
  driver:
    source: github.com/hyve-modules/eks
    version: v1.0.0
  params:
    instance_type: t3.medium
    node_count: "3"
  workflows:
    onCreate:
      - deploy-monitoring
    onDelete:
      - drain-workloads
```

This is the same shape whether it's a local file or a real Kubernetes custom resource — `kubectl apply -f` works on it unmodified once `hyve` is deployed as a cluster-native controller.

<div style={{ textAlign: 'center', color: 'var(--sl-color-gray-3)', maxWidth: '40rem', margin: '0.5rem auto 0' }}>
  Then `hyve reconcile` makes reality match the directory — locally or from CI. Version it with git and change history, reviews, and rollbacks come free; skip git and it's still the exact same reconcile loop.
</div>

## How it works

From nothing to a running cluster in four commands.

<Steps>
  <Step title="Install a module">
    Modules are versioned, self-contained packages that implement cluster operations — create, delete, status, auth. Each operation is invoked as a shell entry point, but can call out to any language or tool internally — only the output contract matters. No cloud SDKs embedded in Hyve.

    ```bash
    hyve module add github.com/hyve-modules/civo@v1.0.0
    ```
  </Step>

  <Step title="Create a template">
    Templates capture your cluster shape, driver, region, and params. One template, infinite clusters.

    ```bash
    hyve template create my-civo-template \
      --driver github.com/hyve-modules/civo \
      --driver-version v1.0.0 \
      --region PHX1 \
      --set node_size=g4s.kube.medium
    ```
  </Step>

  <Step title="Create a cluster from the template">
    Create a cluster from the template to generate a cluster YAML in your active environment's directory.

    ```bash
    hyve cluster create my-cluster --template my-civo-template
    ```
  </Step>

  <Step title="Reconcile">
    Reconcile reads the directory, calls the module, and provisions the cluster. The same command works locally or from a CI/CD pipeline.

    ```bash
    hyve reconcile
    ```
  </Step>
</Steps>

## Features

Everything needed to run cluster fleets from a local directory — nothing you have to operate on the side.

<CardGroup cols={3}>
  <Card title="Declarative, Optionally Git-Backed" icon="code-branch" color="#3b82f6">
    Cluster state is plain YAML in a directory. Version it with git for change history, PR review, and revert-to-rollback — or skip git entirely and reconcile a plain local directory.
  </Card>

  <Card title="Module System" icon="cube" color="#8b5cf6">
    Modules are versioned packages for any cloud provider. Install once, reference from any template. No cloud SDKs are bundled into Hyve itself.
  </Card>

  <Card title="Lifecycle Hooks" icon="gears" color="#22c55e">
    `beforeCreate`, `onCreate`, `afterCreate`, `onDelete`, `afterDelete` — run arbitrary workflows at each stage of a cluster's lifecycle, automatically, every time.
  </Card>

  <Card title="Cluster Templates" icon="file-code" color="#06b6d4">
    Define the shape of a cluster once. Execute the template by name to stamp out clusters consistently across environments.
  </Card>

  <Card title="Variable Injection" icon="brackets-curly" color="#eab308">
    Module params are injected as `HYVE_PARAM_*` environment variables. Workflow outputs flow back as `HYVE_KEY=value` lines and are persisted for the next reconcile.
  </Card>

  <Card title="Two Run Modes, One Engine" icon="server" color="#ef4444">
    `hyve reconcile` against a local/git directory, or deploy Hyve as a cluster-native controller + API for team/multi-tenant use — the same reconcile engine and the same YAML either way.
  </Card>
</CardGroup>

## Why Hyve?

Think of it as a declarative, reconciliation-based Terraform built specifically for clusters — and designed to work alongside the IaC you already have.

<AccordionGroup>
  <Accordion title="Keep your existing Terraform — Hyve drives the cluster, your IaC drives the rest">
    Hyve doesn't replace Terraform for VPCs, IAM roles, or databases — it's purpose-built for the cluster lifecycle and runs alongside general-purpose IaC. A `beforeCreate` workflow can run `terraform init && terraform apply` to provision supporting infrastructure before the cluster comes up, and `afterDelete` can tear it down. See [Why Hyve](/docs/why-hyve) for the full comparison.
  </Accordion>

  <Accordion title="A local directory as state — no S3, no Terraform Cloud">
    Terraform requires a remote state backend with locking, versioning, and access control. Hyve's state is just a local directory. Add git and full change history, pull request review, and rollbacks come for free — no extra backend to operate or pay for either way, git or not.
  </Accordion>

  <Accordion title="Continuous reconciliation — no plan files or approval gates">
    Add a cluster YAML and it is created. Remove it and the cluster is deleted. Update a field and Hyve reconciles the difference. The same `hyve reconcile` command handles all three cases without separate plan/apply cycles.
  </Accordion>

  <Accordion title="Lifecycle hooks — no extra orchestration">
    After a cluster becomes active, you typically need to install controllers, run smoke tests, or notify a team channel. In Hyve, `onCreate` and `onDelete` hooks run automatically as part of the same reconcile cycle — define once, run every time.
  </Accordion>

  <Accordion title="Modules, not embedded SDKs">
    Old-style GitOps tools embed cloud SDKs. If your provider isn't supported, you're stuck. Hyve modules are external packages that shell out to any CLI tool you already have configured. Your credentials, your tools, your control.
  </Accordion>

  <Accordion title="Automatic kubeconfig management">
    Run `hyve cluster auth my-cluster` to merge any cluster's kubeconfig into `~/.kube/config`. The module handles the authentication call; Hyve manages the merge.
  </Accordion>

  <Accordion title="Native cluster templates — no Terragrunt required">
    For teams provisioning clusters to the same pattern, Hyve has native templates: define the shape once, instantiate by name. The resulting cluster YAML is committed, reconciled, and `onCreate` workflows run — all in one step.
  </Accordion>
</AccordionGroup>

## Get started

Pick your entry point — the quickstart takes about five minutes.

<CardGroup cols={2}>
  <Card title="Quick Start" icon="rocket" href="/docs/quickstart" color="#3b82f6">
    Up and running in under 5 minutes
  </Card>

  <Card title="Modules" icon="cube" href="/docs/concepts/modules" color="#8b5cf6">
    Learn how the module system works
  </Card>

  <Card title="CLI Reference" icon="terminal" href="/docs/cli/overview" color="#10b981">
    Complete command reference
  </Card>

  <Card title="Guides" icon="map" href="/docs/guides/cluster-management" color="#f97316">
    Cluster management guides
  </Card>

  <Card title="hyve-sample-repo" icon="flask" href="https://github.com/cbridges1/hyve-sample-repo" color="#ec4899">
    Clone a complete, runnable example — no cloud credentials needed
  </Card>

  <Card title="Environments" icon="folder" href="/docs/cli/env" color="#eab308">
    Point hyve at a plain local directory, no git required
  </Card>
</CardGroup>
