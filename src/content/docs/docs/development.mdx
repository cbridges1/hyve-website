---
title: "'Development'"
description: "'Preview changes locally to update your docs'"
---

import Steps from '../../../components/mdx/Steps.astro';
import Step from '../../../components/mdx/Step.astro';
import Accordion from '../../../components/mdx/Accordion.astro';
import AccordionGroup from '../../../components/mdx/AccordionGroup.astro';
import Info from '../../../components/mdx/Info.astro';
import Frame from '../../../components/mdx/Frame.astro';

<Info>
  **Prerequisites**:
  - Node.js version 18.17 or higher (check with `node --version`)
  - A clone of this docs repository
</Info>

This site is built with [Astro](https://astro.build) and [Starlight](https://starlight.astro.build). Follow these steps to preview changes locally.

<Steps>
<Step title="Install dependencies">

```bash
npm install
```
</Step>

<Step title="Start the dev server">

```bash
npm run dev
```

A local preview of the docs will be available at `http://localhost:4321`, with hot reload on every save.

</Step>
</Steps>

## Custom ports

By default, Astro uses port 4321. If it's already in use, Astro automatically tries the next available port and prints the one it picked. To force a specific port:

```bash
npm run dev -- --port 3333
```

## Production build

To build the static site and preview the production output locally:

```bash
npm run build
npm run preview
```

`npm run build` writes the static site to `dist/`. `npm run preview` serves that output locally so you can sanity-check the production build before pushing — this is also exactly what the deploy workflow runs in CI.

## Deployment

Pushing to `main` triggers the GitHub Actions deploy workflow (`.github/workflows/deploy.yml`), which builds the site and publishes it to GitHub Pages. If the deployment is successful, you should see the following:

<Frame>
  <img src={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/images/checks-passed.png`} alt="Screenshot of a deployment confirmation message that says All checks have passed." style={{ borderRadius: '0.5rem' }} />
</Frame>

## Code formatting

We suggest using extensions on your IDE to recognize and format MDX. If you're a VSCode user, consider the [MDX VSCode extension](https://marketplace.visualstudio.com/items?itemName=unifiedjs.vscode-mdx) for syntax highlighting, and [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) for code formatting.

## Troubleshooting

<AccordionGroup>
  <Accordion title='Error: Could not load the "sharp" module using the darwin-arm64 runtime'>

    This may be due to an outdated version of Node. Try the following:
    1. Delete `node_modules` and `package-lock.json`
    2. Upgrade to Node 18.17+ (or the latest LTS)
    3. Reinstall dependencies: `npm install`
  </Accordion>

  <Accordion title="Issue: stale content after pulling changes">

    Solution: Astro caches content collection metadata under `.astro/`. Delete it and restart the dev server:
    ```bash
    rm -rf .astro
    npm run dev
    ```
  </Accordion>
</AccordionGroup>

Curious about what changed in a given Astro/Starlight release? Check the [Astro changelog](https://github.com/withastro/astro/blob/main/packages/astro/CHANGELOG.md) or the [Starlight changelog](https://github.com/withastro/starlight/blob/main/packages/starlight/CHANGELOG.md).
