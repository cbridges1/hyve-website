---
title: "Installation"
description: "Install and set up Hyve on your system"
---

import Card from '../../../components/mdx/Card.astro';
import CardGroup from '../../../components/mdx/CardGroup.astro';
import Tabs from '../../../components/mdx/Tabs.astro';
import Tab from '../../../components/mdx/Tab.astro';
import Steps from '../../../components/mdx/Steps.astro';
import Step from '../../../components/mdx/Step.astro';
import Accordion from '../../../components/mdx/Accordion.astro';
import AccordionGroup from '../../../components/mdx/AccordionGroup.astro';
import Note from '../../../components/mdx/Note.astro';

## Prerequisites

<CardGroup cols={2}>
  <Card title="Go 1.21+" icon="code" color="#6366f1">
    [Install Go](https://golang.org/doc/install)
  </Card>

  <Card title="Git (optional)" icon="git" color="#8b5cf6">
    Not required — hyve has no built-in git functionality at all. Install it only if you want your state directory version-controlled, using your own `git` commands.
  </Card>
</CardGroup>

<Note>
  **Cloud provider CLI:** Hyve does not bundle cloud SDKs. Install the CLI for each provider you plan to use (AWS CLI, gcloud, az, civo CLI) and authenticate before running Hyve. See [Quick Start](/docs/quickstart) for per-provider setup.
</Note>

## Install

<Steps>
  <Step title="Install with Go">
    ```bash
    go install github.com/cbridges1/hyve@latest
    ```

    This installs the `hyve` binary to your `$GOPATH/bin` directory (usually `~/go/bin`).
  </Step>

  <Step title="Ensure Go bin is in PATH">
    ```bash
    export PATH=$PATH:$(go env GOPATH)/bin
    ```

    Add this line to your shell profile (`~/.bashrc`, `~/.zshrc`, etc.) to make it permanent.
  </Step>

  <Step title="Verify installation">
    ```bash
    hyve --help
    ```

    You should see the Hyve help output listing all available commands.
  </Step>
</Steps>

## Git (optional)

Hyve has no built-in git functionality — no clone, pull, push, or commit. `hyve env create --path <dir>` points hyve at any local directory directly; whether that directory is a git working tree is entirely up to you. If you do want it version-controlled, that's your own `git` CLI, same as any other project:

```bash
git --version   # verify it's installed, if you're using it
```

See [Environments](/docs/concepts/repositories) and [Version Control](/docs/concepts/gitops) for how this fits together.

## Cloud Provider Setup

Hyve uses each provider's native authentication. No credentials are stored in Hyve — your cloud CLI or environment variables are the sole credential source.

<Tabs>
  <Tab title="Civo">
    ```bash
    # Authenticate with the Civo CLI (writes ~/.civo.json)
    civo apikey save my-token YOUR_CIVO_TOKEN
    civo apikey use my-token

    # Or via environment variable
    export CIVO_TOKEN=your_civo_api_token
    ```

    The Civo module reads the active token from `~/.civo.json` or `CIVO_TOKEN` automatically.
  </Tab>

  <Tab title="AWS">
    ```bash
    # AWS CLI (recommended for local dev)
    aws configure

    # Or via environment variables
    export AWS_ACCESS_KEY_ID=your_access_key
    export AWS_SECRET_ACCESS_KEY=your_secret_key
    export AWS_REGION=us-east-1
    ```

    The EKS module uses the standard AWS credential chain.
  </Tab>

  <Tab title="GCP">
    ```bash
    # Application Default Credentials (recommended for local dev)
    gcloud auth application-default login

    # Or via service account
    export GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
    ```
  </Tab>

  <Tab title="Azure">
    ```bash
    # Azure CLI (recommended for local dev)
    az login

    # Or via service principal
    export AZURE_TENANT_ID=your_tenant_id
    export AZURE_CLIENT_ID=your_client_id
    export AZURE_CLIENT_SECRET=your_client_secret
    ```
  </Tab>
</Tabs>

## Directory Structure

After installation, Hyve creates:

```
~/.hyve/
├── repositories.db     # Repository configurations
├── temp/               # Temporary kubeconfig files
└── repositories/       # Cloned repository storage
    ├── production/
    │   ├── clusters/
    │   ├── workflows/
    │   ├── templates/
    │   └── hyve.lock
    └── development/
```

### Overriding the data directory

The default `~/.hyve` location can be overridden with the `--home` flag or the `HYVE_HOME` environment variable:

```bash
# Use a custom directory for a single command
hyve --home /tmp/hyve-staging reconcile

# Set it persistently for a shell session
export HYVE_HOME=~/hyve-staging
hyve env create staging --path ~/repos/staging-state

# Or point to an existing directory
HYVE_HOME=/opt/hyve hyve cluster list
```

The `--home` flag takes precedence over `HYVE_HOME`.

## Platform-Specific Notes

<AccordionGroup>
  <Accordion title="macOS">
    On macOS, you may need to allow the binary in Security & Privacy settings:

    1. Run `hyve` once
    2. Go to System Settings → Privacy & Security
    3. Click "Allow Anyway" for the hyve binary
  </Accordion>

  <Accordion title="Linux">
    No system dependencies required for hyve itself. If you're using a git-backed environment directory (optional — see [Git](#git-optional) above):

    ```bash
    # Debian/Ubuntu
    sudo apt-get install git

    # RHEL/CentOS
    sudo yum install git
    ```
  </Accordion>

  <Accordion title="Windows">
    Variable substitution in workflows uses `cmd /C "command"`. Use `%VAR%` for environment variables alongside `${VAR}` for workflow variables.

    To persist environment variables across sessions, set them via System Properties or add them to your PowerShell profile.
  </Accordion>
</AccordionGroup>

## Verifying Installation

Run these commands to verify everything is set up correctly:

```bash
# Check Hyve installation
hyve --help

# List configured environments (after registering one)
hyve env list
```

## Troubleshooting

<AccordionGroup>
  <Accordion title="Command not found">
    Ensure the `hyve` binary is in your PATH:

    ```bash
    # Check if hyve is in PATH
    which hyve

    # If not, add Go bin to PATH
    export PATH=$PATH:$(go env GOPATH)/bin
    ```
  </Accordion>

  <Accordion title="Permission denied">
    Make the binary executable:

    ```bash
    chmod +x $(which hyve)
    ```
  </Accordion>

  <Accordion title="Go binary not found after install">
    Ensure `$(go env GOPATH)/bin` is in your PATH. Add to your shell profile:

    ```bash
    echo 'export PATH=$PATH:$(go env GOPATH)/bin' >> ~/.zshrc
    source ~/.zshrc
    ```
  </Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Quick Start" icon="rocket" href="/docs/quickstart" color="#3b82f6">
    Create your first cluster
  </Card>

  <Card title="Module System" icon="cube" href="/docs/concepts/modules" color="#8b5cf6">
    Learn how modules work
  </Card>

  <Card title="CLI Reference" icon="terminal" href="/docs/cli/overview" color="#10b981">
    Explore all commands
  </Card>

  <Card title="Git Management" icon="git" href="/docs/guides/git-management" color="#f97316">
    Set up repositories
  </Card>
</CardGroup>
